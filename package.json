{
  "name": "Klinfo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf apps/**/.next",
    "setup": "pnpm run clean && pnpm install && pnpm --filter backend run build && pnpm --filter frontend run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.11.0",
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "pg": "^8.16.0"
  },
  "devDependencies": {
    "@types/nprogress": "^0.2.3"
  }
}
