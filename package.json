{
  "name": "Klinfo",
  "private": true,
  "scripts": {
    "version": "1.0.0",
    "test": "echo \"Error: no test specified\" && exit 1",
    "setup": "pnpm install && pnpm --filter backend run build && pnpm --filter frontend run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.11.0",
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "pg": "^8.16.0"
  },
  "devDependencies": {
    "@types/nprogress": "^0.2.3"
  }
}
